# Workout Tracker

A static MVP workout tracker built with HTML, Tailwind via CDN, and vanilla JavaScript.

## Quick start

Open `index.html` directly in your browser, or serve the folder with any static file server.

## Structure

- `index.html` — App shell.
- `src/data.js` — Schedule data.
- `src/app.js` — UI rendering and localStorage persistence.
